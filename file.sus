main ( ) {
    int n = 5; // Input from fibonacci
    int i = 0;
    int j = 1;
    int k = 0;

    loop (k < n) { // traduccio: for (k = 0; k < n; k++)
        int t;
        t = i + j;
        i = j;
        j = t;
        k = k + 1;
    }

    // Solution from fibonacci is j
}